# Character Setup

How to create and manage a character profile, set attribute values, and persist characters between sessions.

- Create a character in the Character Setup screen.
- Assign ability scores, health, and starting equipment using the UI prefabs.
- Save and load profiles from `Profile/` (ProfileDB).
