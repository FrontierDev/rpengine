# Unit Control

Control a single unit: apply damage/heal, add/remove auras, and adjust initiative.

- Use the Unit Control panel to apply changes locally or broadcast them to players.
- Unit actions call into `Core/Unit.lua` and `Event` handlers.
