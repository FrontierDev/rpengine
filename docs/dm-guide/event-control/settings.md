# Event Settings

Manage global event settings such as turn timers, UI visibility rules, and default palettes.

- Settings are exposed in the Event Control panel.
- Many settings are persisted in the Profile DB.
