# Item Editor

The Item Editor lets DMs create and edit items stored in `Data/`.

- Fields: id, icon, name, stats, usage effects.
- Persistence: items are saved to dataset files and loaded by the ruleset.
