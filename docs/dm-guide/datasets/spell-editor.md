# Spell Editor

The Spell Editor provides an interface to author and test spells used by players and NPCs.

- Define actions, amounts, school, cast conditions and ranks.
- Use `Core/SpellActions.lua` as runtime reference for how actions are resolved.
