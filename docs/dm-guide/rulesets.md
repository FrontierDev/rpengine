# Rulesets

Rulesets control NPC stats, action lists, and default behaviors for an event.

- Store rules in `Profile/RulesetDB.lua` and default rules in `Data/`.
- Use the Ruleset Editor to adjust values per table or campaign.
